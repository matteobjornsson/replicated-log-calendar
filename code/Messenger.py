import socket
import threading

"""
class Messenger:

    connectionList = []
    threadList = []

    def __init__(self, N: list):
        '''
        takes in list of tuples that represent other nodes in system
        (nodeID: int, nodeIP: str)

        creates that many socket connections and stores them in a list
        '''

        # initialize socket connections, store them in list

    # need a thread that listens for new connections on a port and adds them
    # to the connection list.
"""